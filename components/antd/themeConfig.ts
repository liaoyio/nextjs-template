import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 14,
    colorPrimary: "#10b981",
  },
  components: {
    Button: {
      fontWeight: 300,
    },
  },
};

export default theme;
